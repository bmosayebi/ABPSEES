# وابستگی‌های کامل رابط گرافیکی ABPSEES (نصب داخل UI/venv)
#
#   cd UI
#   source venv/bin/activate
#   pip install -r requirements.txt
#
# bitsandbytes فقط برای آموزش CUDA لازم است؛ روی Mac برای inference نصب نمی‌شود.

# --- UI Persian shaping ---
arabic-reshaper>=3.0
python-bidi>=0.4.2
pillow>=10.0

# --- Core ML stack (CPU / Apple Silicon inference) ---
torch>=2.1
transformers>=4.40
peft>=0.10
accelerate>=0.28
safetensors>=0.4
sentencepiece>=0.1.99
protobuf>=4.0

# --- Project extras used by load/predict path ---
pyyaml>=6.0
numpy>=1.26
scipy>=1.12
scikit-learn>=1.4
pandas>=2.0
tqdm>=4.66
huggingface_hub>=0.23
